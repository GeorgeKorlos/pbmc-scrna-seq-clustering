normalization:
  target_sum: 10000
  log_transform: true

scaling:
  regress_out: ["pct_counts_mt"] 

de_analysis:
  test: "wilcoxon"
  correction: "benjamini-hochberg"
  min_log2fc: 0.25
  min_pct: 0.25
  alpha: 0.05

random_seed: 42

reproducibility:
  deterministic: true